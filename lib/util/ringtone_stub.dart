/// No-op ring tones for platforms without a Web Audio implementation
/// (tests run here, where a plugin-free no-op is exactly right).
void ringBurst({required bool incoming}) {}

void stopTone() {}
