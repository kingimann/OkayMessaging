include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    prefer_final_fields: true
    avoid_print: true

analyzer:
  exclude:
    - lib/preview.dart # QA-only entrypoint, not shipped
  errors:
    invalid_annotation_target: ignore
